---
import Layout from "../layouts/Layout.astro";
import SectionHeader from "../components/SectionHeader.astro";
import ContactForm from "../components/ContactForm.astro";
import { site, getWhatsAppUrl } from "../data/site";
---

<Layout title="Contatti | GED Group" description="Contatta GED Group per richiedere un preventivo o un sopralluogo.">
  <section class="mx-auto max-w-7xl px-4 py-12">
    <SectionHeader
      eyebrow="Contatti"
      title="Parliamo del tuo servizio"
      subtitle="Pagina contatti già impostata per conversione. Quando colleghiamo API + Turnstile diventa operativa al 100%."
    />

    <div class="mt-8 grid gap-6 lg:grid-cols-[0.95fr_1.05fr]">
      <div class="space-y-4">
        <div class="panel p-5">
          <h2 class="text-xl font-semibold">Recapiti</h2>
          <ul class="mt-4 space-y-3 text-sm text-neutral-600">
            <li>
              <div class="font-medium text-neutral-900">Telefono</div>
              <a href={`tel:${site.phoneHref}`} class="hover:underline" data-track="click_phone">{site.phoneDisplay}</a>
            </li>
            <li>
              <div class="font-medium text-neutral-900">Email</div>
              <a href={`mailto:${site.email}`} class="hover:underline" data-track="click_email">{site.email}</a>
            </li>
            <li>
              <div class="font-medium text-neutral-900">WhatsApp</div>
              <a href={getWhatsAppUrl()} target="_blank" rel="noopener" class="hover:underline" data-track="click_whatsapp">
                Chatta con noi
              </a>
            </li>
            <li>
              <div class="font-medium text-neutral-900">Sede</div>
              <span>{site.address}</span>
            </li>
          </ul>
        </div>

        <div class="panel p-5">
          <h2 class="text-xl font-semibold">Sopralluogo</h2>
          <p class="mt-2 text-sm leading-6 text-neutral-600">
            Inseriamo qui il testo reale su aree coperte, tempi medi di risposta e informazioni utili prima del sopralluogo.
          </p>
          <div class="mt-4 rounded-xl border border-black/5 bg-neutral-50 p-3 text-sm text-neutral-600">
            Placeholder area di servizio: Provincia di Latina / Roma / ecc.
          </div>
        </div>

        <div class="panel p-5">
          <h2 class="text-xl font-semibold">Mappa</h2>
          <div class="mt-3 grid h-48 place-items-center rounded-xl border border-dashed border-neutral-300 bg-neutral-50 text-sm text-neutral-500">
            Qui inseriamo Google Maps embed
          </div>
        </div>
      </div>

      <ContactForm />
    </div>
  </section>
</Layout>