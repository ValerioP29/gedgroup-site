---
import Layout from "../layouts/Layout.astro";
import InternalPageHero from "../components/InternalPageHero.astro";
import SectionHeader from "../components/SectionHeader.astro";
import FinalCtaBand from "../components/FinalCtaBand.astro";
---

<Layout title="Chi siamo | GED Group" description="Chi siamo, metodo di lavoro e approccio operativo di GED Group.">
  <InternalPageHero
    eyebrow="Chi siamo"
    title="Metodo, affidabilità e organizzazione al centro"
    subtitle="Presentiamo la struttura GED Group con un layout orientato alla fiducia: visione, approccio operativo, numeri e prova di affidabilità."
    breadcrumbs={[{ label: "Home", href: "/" }, { label: "Chi siamo" }]}
  />

  <section class="section-y">
    <div class="site-container grid gap-6 lg:grid-cols-[1.1fr_0.9fr]">
      <div class="space-y-6">
        <article class="panel" data-reveal="fade-up">
          <SectionHeader eyebrow="Mission" title="Supportiamo ambienti ordinati e funzionali" />
          <p class="mt-4 text-sm leading-7 text-slate-600">
            Aiutiamo aziende, amministratori e strutture a mantenere standard costanti con un servizio chiaro, continuativo e misurabile.
            La pagina è pronta per integrare storia aziendale, territori serviti e posizionamento commerciale reale.
          </p>
        </article>

        <article class="panel" data-reveal="fade-up">
          <h2 class="text-xl font-semibold text-slate-900">Valori operativi</h2>
          <div class="mt-4 grid gap-3 md:grid-cols-2" data-stagger="values-grid">
            {[
              ["Affidabilità", "Routine stabili, presenza costante e gestione chiara delle richieste."],
              ["Qualità verificabile", "Controlli periodici e confronto attivo con il cliente."],
              ["Flessibilità", "Programmi adattabili a orari, turni, accessi e stagionalità."],
              ["Comunicazione", "Referente dedicato e canali rapidi per supporto e segnalazioni."],
            ].map(([title, text]) => (
              <div class="card-base bg-slate-50 p-4" data-stagger-item>
                <h3 class="font-medium text-slate-900">{title}</h3>
                <p class="mt-2 text-sm text-slate-600">{text}</p>
              </div>
            ))}
          </div>
        </article>

        <article class="panel" data-reveal="fade-up">
          <h2 class="text-xl font-semibold text-slate-900">Come onboarding di un nuovo cliente</h2>
          <ol class="mt-4 grid gap-3 sm:grid-cols-2" data-stagger="onboarding-grid">
            {[
              "Sopralluogo e analisi ambienti",
              "Proposta operativa e capitolato",
              "Avvio con pianificazione condivisa",
              "Verifiche e ottimizzazioni periodiche",
            ].map((step, idx) => (
              <li class="card-base bg-slate-50 px-3 py-3 text-sm text-slate-600" data-stagger-item>
                <strong class="text-slate-800">{idx + 1}.</strong> {step}
              </li>
            ))}
          </ol>
        </article>
      </div>

      <aside class="space-y-6">
        <div class="panel" data-reveal="fade-left">
          <h2 class="text-xl font-semibold text-slate-900">Numeri aziendali</h2>
          <div class="mt-4 grid gap-3" data-stagger="kpi-about">
            {[
              ["Anni di esperienza", "XX+"],
              ["Clienti attivi", "XXX"],
              ["Interventi mensili", "XXXX"],
            ].map(([label, value]) => (
              <div class="kpi-card" data-stagger-item>
                <p class="text-xs uppercase tracking-wide text-slate-500">{label}</p>
                <p class="mt-1 text-2xl font-semibold text-slate-900">{value}</p>
              </div>
            ))}
          </div>
        </div>

        <div class="panel" data-reveal="fade-left">
          <h2 class="text-xl font-semibold text-slate-900">Team e approccio</h2>
          <p class="mt-3 text-sm leading-7 text-slate-600">
            Spazio pronto per introdurre coordinamento operativo, area qualità e figure chiave del team.
            Il blocco è predisposto per aggiungere foto e ruoli senza cambiare layout.
          </p>
        </div>
      </aside>
    </div>
  </section>

  <FinalCtaBand
    title="Vuoi valutare GED Group per la tua struttura?"
    subtitle="Ti presentiamo un piano operativo con tempi, frequenze e standard qualità in base alle tue esigenze."
    whatsappText="Ciao GED Group, vorrei conoscerci meglio e valutare una collaborazione."
  />
</Layout>
